# SSH Pad 1.6.0

国行切走应用会冻进程，`watch` 输出和 PTY 一起没。1.6.0：

- SSH 跑在独立进程 `:session`
- 循环播放近乎无声的媒体（国行不当成后台冻）
- 悬浮窗保活（需授权）
- 一键打开小米/华为/OPPO/vivo 自启动页
- 终端回滚 1MB，切回应用能看到后台期间的输出
- 不再自动新建 SSH（避免以为还在原来的 watch 里）
