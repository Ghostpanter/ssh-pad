# SSH Pad 1.9.1

平板 SSH / Telnet 终端。GitHub Primer 配色，夜间 / 白天可切换。

- 切回应用后清掉软键盘组字状态，关掉联想 / 智能标点，避免 `cd ..` 变成 `cd cd...`
- 左侧节点卡片：保存主机，在线时预览 CPU / 内存 / 网络
- 中间分割条可拖动，把空间留给终端
- 首次启动保活引导；之后在设置里改（电池无限制、自启动、悬浮窗、自动重连、虚拟按键、字号、状态刷新）
- 多会话、XFTP 双栏、密码 / 密钥、连接失败自动从列表清除（日志保留）
